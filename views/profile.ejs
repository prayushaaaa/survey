<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Profile</title>
  </head>
  <body>
    <img src = "images/<%=user.name%>.png" height="25%" width="25%">
    <h3><%=user.name%></h3>
    <h3>Surveys created: -</h3>
    
    <% for(var i=0; i<survey.length; i++){ %>
      <h2><%=survey[i].question%></h2>
      <ul>
      <%  (survey[i].answers).forEach((answer)=>{ %>
          <li><%=answer%></li>
        <% }) %>
      </ul>  
      <a href="/delete/<%=survey[i].id%>">Delete</a>
      <a href="/update/<%=survey[i].id%>">Update</a>
      -------
  <% } %>

    <br><br><a href="/login">Logout</a>
  </body>
</html>
